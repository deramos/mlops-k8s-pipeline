apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fraud-api.fullname" . }}-test-connection"
  labels:
    {{- include "fraud-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fraud-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
